# Test-only tooling — deliberately NOT in requirements.txt or backend/requirements.txt
# so the Docker image (see Dockerfile) never installs pytest into production.
#
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
pytest>=8.0
pytest-asyncio>=0.24
