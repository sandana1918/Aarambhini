# Backend-only deps (in addition to the root requirements.txt used by the agents)
fastapi>=0.110
uvicorn[standard]>=0.29
motor>=3.4
pydantic>=2.6
python-multipart>=0.0.9
# langgraph-checkpoint-mongodb + pymongo now live in the root requirements.txt
# (used by graph_store.py, which the agents/orchestrator layer depends on).
