google-genai>=1.0.0
langgraph>=1.0,<2.0
langgraph-checkpoint-mongodb>=0.1.0  # durable graph checkpoints in Atlas
pymongo>=4.6                          # sync client for checkpointer + GridFS images
certifi>=2024.0                       # Atlas TLS CA bundle on Windows
pillow>=10.0
python-dotenv>=1.0
requests>=2.31  # Sarvam STT HTTP client

# Standalone Streamlit prototype only (app.py) — NOT imported by the API.
# The deployed service never loads these; kept here so the prototype still runs.
streamlit>=1.36
qrcode>=7.4
